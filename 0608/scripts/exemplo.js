var ida = 10
    if ( ida < 4 ) {
        console.log (`Pequenote ainda não precisas de ir para a escola pois só tens ${ida} anos`)
    } else if ( ida >= 4 && ida < 6 ) {
        console.log (`Podes te inscrever na Pré-Primária pois já tens ${ida} anos`)
    } else if ( ida >= 6 && ida <= 10 ) {
        console.log (`Já te podes inscrever na Primária pois tens ${ida} anos!`)
    } else {
        console.log (`Vai arranjar trabalho Malandro! ${ida}`)
    }